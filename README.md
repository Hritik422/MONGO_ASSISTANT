# MONGO_ASSISTANT
An AI-powered MongoDB Query Generator that converts natural language user inputs into optimized and schema-compliant MongoDB queries. The system intelligently interprets user intent, maps it to predefined database context, and dynamically generates either find or aggregate queries with proper filtering, projections, joins, and limits.
